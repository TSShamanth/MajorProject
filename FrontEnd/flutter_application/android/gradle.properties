org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# If your system JDK is older than 11, set org.gradle.java.home to your JDK 11+ installation path (uncomment and update the path below):
# org.gradle.java.home=C:/Program Files/Java/jdk-17
# Alternatively, install JDK 11 or newer and point JAVA_HOME to it. Gradle requires Java 11+ for some dependencies.
