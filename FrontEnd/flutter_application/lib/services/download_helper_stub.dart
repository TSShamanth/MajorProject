import 'dart:typed_data';

void downloadFile(Uint8List bytes, String downloadName) {
  // This is a stub implementation for non-web platforms.
  // In a real multi-platform app, you might use packages like
  // 'path_provider' and 'open_file' to save and open the file.
  throw UnimplementedError('File download is only supported on the web platform in this version.');
}
